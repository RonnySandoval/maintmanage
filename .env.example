# Copia el contenido a `.env.local` (no se sube a git).
# Google Cloud Console → APIs y servicios → Credenciales → ID de cliente OAuth 2.0
# Tipo: Aplicación web. Orígenes JavaScript autorizados: tu URL (p. ej. http://localhost:5173).
# APIs a habilitar: Gmail API.
VITE_GOOGLE_CLIENT_ID=
